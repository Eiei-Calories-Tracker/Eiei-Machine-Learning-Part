# AWS credentials for DVC S3 remote (used by Airflow tasks and local dvc commands)
AWS_ACCESS_KEY_ID=your_access_key_id
AWS_SECRET_ACCESS_KEY=your_secret_access_key
AWS_DEFAULT_REGION=ap-southeast-1

# Optional: Airflow UID override for Linux hosts
AIRFLOW_UID=50000
# 